<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>de.arbeitsagentur.opdt</groupId>
  <artifactId>keycloak-extension-oid4vp-parent</artifactId>
  <version>0.13.1</version>
  <packaging>pom</packaging>

  <name>keycloak-extension-oid4vp-parent</name>
  <description>Keycloak-Extension for ID-Wallets (EUDI)</description>

  <url>https://github.com/ba-itsys/keycloak-extension-oid4vp</url>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <organization>IT-Systemhaus der Bundesagentur für Arbeit</organization>
      <organizationUrl>https://github.com/ba-itsys</organizationUrl>
    </developer>
  </developers>

  <modules>
    <module>core</module>
    <module>integration-tests</module>
    <module>conformance-tests</module>
  </modules>

  <scm>
    <connection>scm:git:git://github.com/ba-itsys/keycloak-extension-oid4vp.git</connection>
    <developerConnection>scm:git:git@github.com:ba-itsys/keycloak-extension-oid4vp.git</developerConnection>
    <url>https://github.com/ba-itsys/keycloak-extension-oid4vp/tree/main/</url>
  </scm>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <jdk.version>21</jdk.version>

    <!-- The provider module is published standalone and manages its own versions; keep
         keycloak.version aligned with core/pom.xml -->
    <keycloak.version>26.7.3</keycloak.version>
    <keycloak-test-framework.version>26.7.2</keycloak-test-framework.version>
    <!-- Released from keycloak/keycloak-client, whose versions trail the server. The
         quarkus-bom imported by keycloak-parent pins it several releases back, so the newest
         client release is named here instead. -->
    <keycloak-admin-client.version>26.0.12</keycloak-admin-client.version>
    <junit.version>6.1.1</junit.version>
    <assertj.version>3.27.7</assertj.version>
    <logback.version>1.6.3</logback.version>

    <!-- Root and tests modules are never deployed -->
    <maven.deploy.skip>true</maven.deploy.skip>
  </properties>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.15.0</version>
          <configuration>
            <release>${jdk.version}</release>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>3.9.0</version>
        <configuration>
          <formats>
            <format>
              <includes>
                <include>.gitattributes</include>
                <include>.gitignore</include>
              </includes>
              <trimTrailingWhitespace/>
              <endWithNewline/>
              <indent>
                <spaces>true</spaces>
                <spacesPerTab>4</spacesPerTab>
              </indent>
            </format>
          </formats>
          <java>
            <palantirJavaFormat/>
            <importOrder/>
            <removeUnusedImports/>
            <formatAnnotations/>
          </java>
          <pom>
            <sortPom>
              <expandEmptyElements>false</expandEmptyElements>
            </sortPom>
          </pom>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
